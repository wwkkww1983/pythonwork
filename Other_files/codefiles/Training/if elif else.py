#!/usr/bin/env python3
# -*- coding: utf-8 -*-

class student(object):
    def __inf__(self,number,heigt,weigt):
        self.number= number
        self.heigt = heigt
        self.weigt = weigt
ming = student('1',70,80.5)
#    def print_bmi(self):
#        bmi = weigt / heigt**2
#        print('The BMI of %s is %f' % (Ming.name,bmi))
#bmi = student.weigt / student.heigt**2

#print('The BMI of %s is %f' % (Ming.name,bmi))